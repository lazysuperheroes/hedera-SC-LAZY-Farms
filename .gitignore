node_modules
.env
coverage
coverage.json
typechain
env
.env.old
typechain-types

#Hardhat files
cache
artifacts
*.key
yarn.lock
yarn.lock

# Claude Code frameworks (exclude all)
.claude/

# One-off migration scripts (kept local, not tracked)
scripts/interactions/teardownGemBoostCollections.js
